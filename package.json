{
  "name": "web",
  "version": "0.0.1",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "start": "yarn workspace @repo/web run dev",
    "docker:front:serve:dev": "docker-compose -f docker-compose.dev.yml up"
  },
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@types/node": "17.0.8",
    "lerna": "^4.0.0",
    "typescript": "4.5.4"
  }
}
